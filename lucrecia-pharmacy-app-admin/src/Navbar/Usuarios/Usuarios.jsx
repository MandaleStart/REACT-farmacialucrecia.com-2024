import { Link } from "react-router-dom";

const Usuarios = () => {
    return (
        <Link  to="/usuarios" className="Navbar-item">
            Usuarios
        </Link>
    );
};

export default Usuarios;