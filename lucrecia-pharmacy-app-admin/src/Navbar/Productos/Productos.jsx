import { Link } from "react-router-dom";
const Productos = () => {
    return (
        <Link to="/productos" className="Navbar-item">
            Productos
        </Link>
    );
};

export default Productos;